import { PageShell } from '../../../components/page-shell';

export default function LoginPage() {
  return (
    <PageShell title="Login">
      <p className="description">Enter credentials to log in and access protected backend APIs.</p>
    </PageShell>
  );
}
