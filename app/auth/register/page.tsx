import { PageShell } from '../../../components/page-shell';

export default function RegisterPage() {
  return (
    <PageShell title="Register">
      <p className="description">Create a new account and provision access to the app backend.</p>
    </PageShell>
  );
}
